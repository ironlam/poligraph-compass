{
  "buildCommand": "npx expo export --platform web && node scripts/build-vercel.mjs",
  "outputDirectory": ".vercel/output",
  "crons": [
    {
      "path": "/api/cron/sync",
      "schedule": "0 6 * * *"
    }
  ]
}
